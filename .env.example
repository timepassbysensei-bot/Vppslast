# =========================================================================
# View Point Public School — environment variables
# Copy names into Netlify. NEVER commit real secret values to git.
# =========================================================================

# ---- Browser-safe (exposed in the frontend bundle; NOT secret) ----
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SITE_URL=
VITE_TURNSTILE_SITE_KEY=

# ---- Server-only (Netlify Functions ONLY; must never reach the browser) ----
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
ADMIN_NOTIFICATION_EMAIL=
ADMIN_BOOTSTRAP_EMAIL=
TURNSTILE_SECRET_KEY=
CLEANUP_SECRET=
BOOTSTRAP_SECRET=
